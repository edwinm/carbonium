# Record exact versions instead of caret ranges, so dependencies stay pinned.
# Do not add ignore-scripts here: as persistent config it also suppresses the
# pre/post hooks of `npm run` (pretest -> build). Pass --ignore-scripts per command.
save-exact=true
